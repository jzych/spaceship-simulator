interface:
  display_name: "CMake C++ Workflow"
  short_description: "WSL-first CMake and C++ workflow for this repo"
  brand_color: "#0F766E"
  default_prompt: "Inspect CMakeLists.txt first, prefer WSL commands, then configure, build, and verify the target."
policy:
  allow_implicit_invocation: true
